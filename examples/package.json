{
    "author": "David Komer <david.komer@gmail.com>",
    "name": "awsm",
    "version": "0.1.0",
    "repository": "https://github.com/dakom/awsm",
    "license": "(MIT OR Apache-2.0)",
    "scripts": {
        "clean": "rimraf ./dist && npm run _clean",
        "deploy": "npm run clean && mkdir dist && npm run bundle && npm run _deploy:netlify",
		"bundle:ci": "npm run _install && npm run bundle",
        "bundle": "npm run _bundle:all && npm run _copy:all",
        "_clean": "npm-run-all -p _clean:web _clean:renderer",
        "_clean:web": "cd web-demo && npm run clean",
        "_clean:renderer": "cd renderer-demo && npm run clean",
		"_install": "npm-run-all -p _install:web _install:renderer",
		"_install:web": "cd web-demo && npm install",
		"_install:renderer": "cd renderer-demo && npm install",
        "_bundle:all": "npm-run-all -p _bundle:web _bundle:renderer",
        "_bundle:web": "cd web-demo && npm run bundle",
        "_bundle:renderer": "cd renderer-demo && npm run bundle",
        "_copy:all": "npm-run-all -p _copy:web _copy:renderer _copy:static",
        "_copy:web": "cpy **/* ../../dist/web-demo --cwd=web-demo/dist --parents",
        "_copy:renderer": "cpy **/* ../../dist/renderer-demo --cwd=renderer-demo/dist --parents",
        "_copy:static": "cpy **/* ../dist --cwd=_static --parents",
        "_deploy:netlify": "netlify deploy --prod --dir=dist --site=c6d71941-94ea-494c-a5f6-429f4b8feb9e"
    },
    "devDependencies": {
        "cpy": "^7.2.0",
        "cpy-cli": "^2.0.0",
        "cross-env": "^5.2.0",
        "npm-run-all": "^4.1.5",
        "rimraf": "^2.6.3"
    }
}